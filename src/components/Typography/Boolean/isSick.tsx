import React from "react";
type isSickProps = {
  isSickText: boolean;
};

export const isSick = ({ isSickText }: isSickProps) => {
  if (false) {
    return(
       <h1>{isSickText}</h1>
    ) 
  }
};
