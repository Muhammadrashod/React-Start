import React from "react";
type isEmployedProps = {
  isEmployedText: boolean;
};

export const isEmployed = ({ isEmployedText }: isEmployedProps) => {
  if (true) {
    return(
       <h1>{isEmployedText}</h1>
    ) 
  }
};
