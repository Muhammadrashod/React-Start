import React from "react";
type hasHouseProps = {
  hasHouseText: boolean;
};

export const hasHouse = ({ hasHouseText }: hasHouseProps) => {
  if (true) {
    return(
       <h1>{hasHouseText}</h1>
    ) 
  }
};
