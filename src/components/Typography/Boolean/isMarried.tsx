import React from "react";
type isMarriedProps = {
  isMarriedText: boolean;
};

export const isMarried = ({ isMarriedText }: isMarriedProps) => {
  if (false) {
    return(
       <h1>{isMarriedText}</h1>
    ) 
  }
};
