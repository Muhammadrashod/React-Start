import React from "react";
type isStudentProps = {
  isStudentText: boolean;
};

export const isStudent = ({ isStudentText }: isStudentProps) => {
  if (false) {
    return(
       <h1>{isStudentText}</h1>
    ) 
  }
};
